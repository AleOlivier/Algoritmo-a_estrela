Este programa é responsável por implementar o algoritmo a*(a estrela)para fazer com que um agente atravesse um ambiente pré-definido por um mapa em grid. O objetivo é fazer com que o agente chegue a um objetivo final marcado pelo usuário sem colidir com nenhum obstáculo.